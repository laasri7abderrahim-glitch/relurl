{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "functions": {
    "app/api/**/*.{js,ts}": {
      "maxDuration": 30
    },
    "app/[slug]/page.tsx": {
      "maxDuration": 10
    }
  }
}
